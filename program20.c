#include <stdio.h>

int main() {
    int num1 = 15;  // first number
    int num2 = 25;  // second number
    int result;     // variable to store the sum

    result = num1 + num2;  // add and store in result

    printf("The sum is %d\n", result);
    return 0;
}
