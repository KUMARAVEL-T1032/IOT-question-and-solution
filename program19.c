#include <stdio.h>

int main() {
    int one_digit = 7;    // one-digit number (0-9)
    int two_digit = 42;   // two-digit number (10-99)
    int sum = one_digit + two_digit;

    printf("Sum of %d and %d is %d\n", one_digit, two_digit, sum);
    return 0;
}
