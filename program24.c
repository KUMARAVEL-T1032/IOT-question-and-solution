#include <stdio.h>

int main() {
    float a = 7.5f;   // first floating-point number
    float b = 3.2f;   // second floating-point number
    float result = a - b;

    printf("Result of %.2f - %.2f is %.2f\n", a, b, result);
    return 0;
}
